import 'package:flutter/material.dart';

class OwnerDetailLine extends StatelessWidget {
  const OwnerDetailLine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 1,
      height: 50,
      color: Colors.black12,
    );
  }
}
