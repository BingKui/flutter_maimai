import 'package:flutter/material.dart';

class HotItem extends StatelessWidget {
  const HotItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
